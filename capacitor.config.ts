import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.lovable.livestockfarm",
  appName: "Livestock Farm Management",
  webDir: "dist/client",
};

export default config;